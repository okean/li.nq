%p.lead You currently have the preview feature enabled.
= form_tag "/a/preview/1", remote: true, method: :delete, class: "form-inline" do
  %span.help-block Click to disable preview
  = submit_tag 'Disable', class: "btn btn-success btn-primary"
%p.muted The preview feature requires cookies to be enabled in your web browser.